# うのっちナビ v1.1 Sol仕様書・Terra引き継ぎ

作成日: 2026-07-11
対象: `sanpeita.github.io` / うのっちナビ 1.0 → 1.1
工程: Sol（仕様整理・要件判断・構造設計）完了

## 1. 調査結果の要約

- サイトはフレームワークやビルド工程を持たない静的GitHub Pagesで、公開ブランチは `master`。
- うのっちナビは `index.html` 内の右下固定ウィジェットで、外部AI APIやサーバーを使わない。
- 表示ロジックは `assets/chatbot/chatbot.js`、検索と回答選択は `assets/chatbot/search-core.js`、コンテンツは `assets/chatbot/knowledge.json` に分離されている。
- Fuse.js 7.1.0はローカル同梱。日本語検索はFuseだけに依存せず、キーワード一致と質問らしさの判定を併用している。
- 1.0は15 intent、各intent 2回答、関連リンク、次の質問候補を持つ。回答はカードだが、質問と回答をログへ追加するため、利用が長いほどチャット履歴が伸びる。
- 開閉、Escapeで閉じる、外部リンクの安全な別タブ表示、内部アンカー選択時のパネル閉鎖、画像フォールバックを実装済み。
- モバイルは左右12px、最大78dvh、閉じるボタン44px。既知の横幅超過は1.0で修正済み。
- 「V文化論ってなに？」は `v-culture` intentとしてすでに検索可能。
- ワンダーメイクフェス5は `events-awards` intentに登録済み。動画IDは `ZgVxVa9wKUo`、案内位置は1分41秒付近。既存証拠カードと現行データが一致している。
- 現行テストは、15 intent、検索、フォールバック、suggestion解決、回答重複回避、リンク、避けるべき実写・本人性の表現を検証する。
- package管理、Lint、ビルドはない。現行の自動検証入口は `node --test tests/chatbot-search.test.cjs`。

## 2. 現状のv1.0分析

### 現在できること

- 右下のファンアートアイコンからナビを開閉する。
- 初期候補または自由入力から15カテゴリを検索する。
- 回答、関連作品・記事、次の質問をカード表示する。
- 内部アンカーまたは外部サイトへ移動する。
- 質問文を外部へ送らず、ブラウザ内だけで案内する。

### 現在の課題

1. 初見ユーザーは質問を自分で選ぶ必要があり、活動全体のつながりを短時間で理解しにくい。
2. 質問・回答を追記し続けるログ型で、回答カード中心という製品方針に対してスクロール量が増えやすい。
3. 画面状態は `knowledge` と `searchEngine` 程度で、ツアー位置、履歴、モード、復帰先を表現できない。
4. 初期質問がJavaScriptへ直書きされ、コンテンツ編集が `knowledge.json` だけで完結しない。
5. 回答カードにはintentの `title` が出ず、回答の主題をひと目で把握しにくい。
6. 閉じた後の画面はDOM上で保持されるが、再読み込みや再訪の復元方針は定義されていない。

### 再利用する部分

- 右下配置、開閉DOM、既存配色、角丸、影、アバター、フォーム。
- `knowledge.json` の15 intent、facts、answers、links、suggestions。
- `createSearchEngine` と `chooseAnswer`。
- 内部アンカー解決、外部リンクの `target="_blank"` と `rel="noopener noreferrer"`。
- モバイルの左右余白、safe-area、44px閉じるボタン。
- 読み込み失敗表示、画像フォールバック、既存検索テスト。

### 壊れやすい部分

- `INITIAL_QUESTIONS` とsuggestionsは検索文を介してintentへ到達するため、文言変更で別intentへ誤解決しうる。
- `messageLog.append()` を前提にした描画を単一カード表示へ変える際、`role="log"`、aria-live、スクロール処理の意味も合わせて変更が必要。
- 内部リンクは実在アンカーに限定される。新しいアンカーを推測で追加しない。
- モバイル幅は過去に数pxの横超過が起きたため、panel幅とwidget左右指定を同時に崩さない。
- `knowledge.version` とintent件数を固定値で検証しているため、データ拡張時はテスト更新が必要。

## 3. v1.1仕様書

### 3.1 ゴール

v1.1は、質問を考えなくても「うのっちは何者で、制作・教育・V文化論がどうつながっているか」を7枚以内の回答カードで把握できる案内体験を提供する。

1.0との差は、検索カテゴリの増量ではなく、既存intentを順序づけて読む「おすすめツアー」と、ログを積まない単一回答カード型への整理である。

### 3.2 v1.1で実現すること

- 初期画面の最優先操作として「おすすめツアーをはじめる」を表示する。
- 既存intentを参照する7ステップの標準ツアーを追加する。
- ツアー中に「次を見る」「ひとつ前に戻る」「ツアーを終了」を操作できる。
- ツアー終了時に「別のテーマを見る」と「最初から見る」を提示する。
- 個別質問も従来どおり利用できる。
- 個別回答・ツアーとも、主題タイトル、本文、補足的な進捗、関連リンク、次の行動を1枚のカードにまとめる。
- 「V文化論ってなに？」を初期テーマに昇格し、ツアーにも含める。
- ワンダーメイクフェス5動画を、教育・toio・ものづくり・イベント実績のつながりとして自然に紹介する。
- 閉じて同一ページ内で再度開いた場合は、そのカードとツアー位置を保持する。
- ページ再読み込み後は初期画面へ戻す。再訪追跡や永続化は行わない。

### 3.3 v1.1では実現しないこと

- 外部AI API、生成回答、質問送信、会話履歴保存、ユーザープロファイル化。
- 複数の分岐型ツアー、閲覧者属性別パーソナライズ、進捗のlocalStorage保存。
- 動画埋め込み、モーダル動画プレイヤー、サイト本文の新規セクション追加。
- 既存15 intentの全面的な文章刷新や大規模デザイン変更。
- 利用分析、Cookie、外部トラッキング。

### 3.4 想定ユーザーと価値

| 閲覧者 | 最初に知りたいこと | v1.1での主導線 |
|---|---|---|
| 初めて知る人 | 何をしている人か | おすすめツアー |
| VTuber事務所・選考関係者 | 独自性、継続性、チーム貢献 | ツアー → VTuber → 制作 → 実績 |
| 教育関係者 | 教材設計、子どもとの活動 | ツアー → 教育 → ワンダーメイクフェス5 |
| 技術・ものづくり関心層 | Unity、Blender、Roblox、toio、デジファブ | 個別テーマまたはツアー制作カード |
| 既存視聴者 | V文化論や代表作への近道 | 初期テーマ → 関連リンク |

### 3.5 標準おすすめツアー

ツアーは全活動の網羅ではなく、人物像と活動のつながりを理解する最短経路とする。既存intentを再利用し、ツアー固有本文を大量に複製しない。

| 手順 | 参照intent | カード見出しの役割 | 主な関連導線 |
|---:|---|---|---|
| 1/7 | `profile-overview` | まず、うのっちについて | `#about` |
| 2/7 | `vtuber-history` | VTuberを表現の器に | YouTube / note |
| 3/7 | `representative-works` | 遊びと技術を作品に | `#works` |
| 4/7 | `digital-education` | 難しそうを、学べそうに | Blender Shorts |
| 5/7 | `digital-fabrication` | 画面の中から、手で触れるものへ | `#works` |
| 6/7 | `v-culture` | 活動をつなぐV文化論 | note |
| 7/7 | `events-awards` | 教育・toio・実績が交わる場所 | ワンダーメイクフェス5動画 |

確定ルール:

- ステップ3の本文または補足でUnity、Blender、Roblox、Minecraft、toioの広がりを一文で示す。各技術を別ステップにしてツアーを長くしない。
- TED世界共創計画はステップ6のV文化論カードで関連テーマとして短く触れる。1.1では独立intentを新設しない。
- ワンダーメイクフェス5の紹介は「toioブースで子どもたちと活動する様子を記録した公式ブースレポート」とする。「本人の実写」「顔出し」「中の人」等は使わない。
- 動画リンクは `https://www.youtube.com/watch?v=ZgVxVa9wKUo&t=101s` を継続使用する。
- ツアー中に関連リンクを開いてもツアー位置は同一ページ内メモリに保持する。内部リンクはパネルを閉じ、外部リンクは別タブで開く。

### 3.6 初期画面とコンテンツ

初期メッセージ:

> こんにちは。うのっちの活動を、作品・教育・V文化論のつながりからご案内します。初めての方は、おすすめツアーからどうぞ。

初期操作の優先順:

1. `おすすめツアーをはじめる`（主ボタン）
2. `うのっちは何をしている人？`
3. `代表作品を見せて`
4. `V文化論ってなに？`
5. `教育活動について教えて`

自由入力は維持するが、初期候補より下に置く。文体は、文化系・教育系・フレンドリーを基本に、断定しすぎず、1カード本文は日本語160〜240字程度を上限目安とする。リンクは原則3件以内、関連テーマは3件以内。

### 3.7 画面構成

パネルは次の4領域を維持・整理する。

1. ヘッダー: アバター、名称、モード表示（通常案内 / おすすめツアー）、閉じる。
2. カード表示領域: 常時1枚の主カード。過去のチャットログは残さない。
3. カード操作領域: ツアーでは戻る・次へ・終了、通常回答では関連テーマ。
4. 質問フォームと免責: 従来どおり。

カード要素:

- eyebrow: `おすすめツアー 3 / 7` または `PORTFOLIO GUIDE`
- title: intent.titleまたはツアー用見出し
- body: 回答本文
- links: 関連作品・記事
- suggestions/actions: 次のテーマまたはツアー操作

### 3.8 ユーザーフロー

```text
閉じた状態
  → 開く
    → 初期カード
      ├─ おすすめツアー開始 → step 1
      │   ├─ 次へ → step n+1
      │   ├─ 戻る → step n-1
      │   ├─ 関連リンク → 内部移動または外部別タブ
      │   └─ 終了 → ツアー終了カード
      │                    ├─ 最初から
      │                    └─ 別テーマを見る → 初期カード
      ├─ 初期テーマ選択 → 個別回答カード
      └─ 自由入力 → 検索結果またはフォールバックカード

任意の開いた状態 → 閉じる → 同一ページ内で再度開く → 直前カードを復元
ページ再読み込み → 初期カード
```

個別質問をツアー中に送信した場合は、暗黙にツアーを終了して個別回答へ移る。確認ダイアログは出さない。ツアーへ戻る履歴は保持しない。

### 3.9 機能一覧

| 機能 | 目的 | 入力・操作 | 出力・状態 | 例外 | 優先度 |
|---|---|---|---|---|---|
| 開閉 | 閲覧を邪魔せず案内 | launcher / close / Escape | `closed` / `open` | 読込前でも閉じられる | 必須 |
| 初期カード | 迷わず開始 | ナビを開く | ツアー主導線と4テーマ | データ失敗時は再読込案内 | 必須 |
| ツアー開始 | 活動全体を案内 | 主ボタン | `tour`, index 0 | tour定義不正なら初期へ | 必須 |
| 前後移動 | 読み進め・見直し | 戻る / 次へ | index更新、単一カード差替え | 範囲外へ進ませない | 必須 |
| ツアー終了 | 途中離脱可能にする | 終了 | 終了カード、tour解除 | なし | 必須 |
| 個別質問 | 従来機能維持 | 候補 / 自由入力 | 回答カード | 不一致はfallback | 必須 |
| 関連リンク | 詳細へ回遊 | link選択 | 内部移動 / 外部別タブ | URL不正は描画しない | 必須 |
| 同一ページ復元 | 閉じても迷子にしない | 閉じて再度開く | 現在カード維持 | reload時は初期化 | 必須 |
| カード切替 | 状態変化を理解しやすく | 任意の遷移 | 軽いfade/slide | reduced-motion時は抑制 | 必須 |

### 3.10 状態モデル（概念）

Terraは具体名を決めてよいが、少なくとも次を表現する。

- panel: `closed | open`
- view: `loading | welcome | answer | tour | tour-complete | error`
- currentIntentId: stringまたはnull
- tourId: `recommended` またはnull
- tourIndex: 0〜6またはnull
- previousAnswerIds: intentごとの直前回答ID（既存挙動維持）

状態はページ内メモリだけに置く。URL hash、History API、localStorage、sessionStorageは1.1では使わない。

### 3.11 UI・UX・アクセシビリティ方針

- 現在の色、フォント、アバター、角丸、影を継承し、一般的なAIチャットの吹き出し列をやめる。
- panel寸法とモバイル左右12pxを維持。常時全画面化しない。
- 主操作は44px以上のタッチ領域を確保する。戻る・次へ・終了は意味が色だけに依存しない。
- カード切替は150〜220ms程度の軽いopacity + 数px移動に留める。`prefers-reduced-motion` では実質無効化する。
- カード差替え後はカード見出しへプログラム的にフォーカスを移さず、表示領域先頭へスクロールし、aria-liveで要約を通知する。入力中の予期しないフォーカス移動を避ける。
- 単一カード化に合わせ、`role="log"` は使用しない。カード領域は `aria-live="polite"`、`aria-atomic="true"` を候補とする。
- launcherの `aria-expanded`、`aria-controls`、Escape、閉じた後launcherへ戻す挙動を維持する。
- ツアー進捗は視覚表示と読み上げ可能なテキストの両方で示す。
- 内部リンクで閉じる際は既存と同様にlauncherへ戻す。ただしページ内アンカー移動を妨げないか実機確認する。

### 3.12 読み込み・エラー

- knowledge取得中は既存の読み込み表示とフォーム無効化を維持する。
- JSON取得失敗、必要キー欠落、tour参照intent欠落はコンソールだけで終わらせず、「読み込めませんでした。ページを再読み込みしてください」のカードを表示する。
- tour内の1 intentだけ欠けた場合に黙って飛ばさない。データ整合テストで公開前に検出する。
- 不正または空の外部URLは描画しない。公開前テストで失敗させる。

## 4. 技術方針

### 4.1 構造

全面刷新はしない。依存追加や新規フレームワーク導入も行わない。

- `knowledge.json`: `version` を1.1系へ更新し、初期画面設定と `tours` を追加する。intent本文は既存を再利用し、必要最小限だけ調整する。
- `chatbot.js`: append型ログ描画を、view stateから単一カードを再描画する方式へ整理する。検索処理は引き続きsearch-coreへ委譲する。
- `search-core.js`: 原則変更しない。tourデータの純粋関数をここへ混ぜない。必要なら小さな `tour-core.js` を新設し、ブラウザとNodeテストで共有する。
- `index.html`: data hook、カード領域のARIA、必要ならtour-core script読込を最小変更する。
- `styles.css`: 既存クラスを再利用し、カード見出し、進捗、tour操作、切替アニメーションを追加する。
- `tests`: 検索テストを維持し、tour定義・全ステップ参照・操作境界を追加検証する。

推奨は `assets/chatbot/tour-core.js` の新設。ツアー配列検証、index境界、現在step取得をDOMから分離でき、LunaがNodeテストを書きやすい。過剰なclassや汎用state libraryは不要。

### 4.2 データ方針

Terraは最終形を設計するが、次の意味構造を守る。

```json
{
  "version": "1.1.0",
  "welcome": {
    "message": "...",
    "primaryAction": { "type": "tour", "tourId": "recommended", "label": "おすすめツアーをはじめる" },
    "intentIds": ["profile-overview", "representative-works", "v-culture", "digital-education"]
  },
  "tours": [
    {
      "id": "recommended",
      "title": "うのっちを知るおすすめツアー",
      "steps": [
        { "intentId": "profile-overview", "heading": "まず、うのっちについて" }
      ]
    }
  ],
  "intents": []
}
```

ツアーstepはintent ID参照を基本にし、回答本文を複製しない。ツアー固有の短い見出し・補足は許可する。初期質問の文言を検索へ再投入する方式はやめ、可能な操作はintent IDを直接指定する。

### 4.3 テスト方針

最低限、次をNodeテストで自動化する。

1. versionが `1.1.0`。
2. 既存15 intent IDが維持され、重複がない。
3. welcomeが参照するintentが存在する。
4. recommended tourが7 stepで、全intent参照が存在し、step見出しが空でない。
5. 最初で戻れず、最後の次で完了へ進み、範囲外indexを作らない。
6. 「V文化論ってなに？」とワンダーメイクフェス5質問が引き続き正しいintentへ解決する。
7. 動画ID `ZgVxVa9wKUo` と `t=101s` が保持される。
8. 禁止表現テストを維持する。
9. 内部アンカーが実在し、外部URLが構文上有効。
10. 全suggestionが解決する既存ゲートを維持する。

手動確認:

- desktopと390×844相当で、ツアー完走、戻る、途中終了、再開、個別質問、閉じて再度開くを確認。
- 320〜380px相当で横スクロールが出ない。
- キーボードのみで全操作できる。
- Escapeとフォーカス復帰、reduced-motionを確認。
- `index.html` の主要セクションと既存外部リンクが壊れていない。

## 5. 変更対象ファイル一覧

| ファイル | 区分 | 変更理由 | Terraが確認する点 |
|---|---|---|---|
| `assets/chatbot/knowledge.json` | 変更 | version、welcome、tours、最小限の文言追加 | スキーマ、7step、既存15 intent維持 |
| `assets/chatbot/chatbot.js` | 変更 | 単一カード描画、tour状態と操作 | DOM責務、フォーカス、閉じた際の状態保持 |
| `assets/chatbot/tour-core.js` | 新規候補 | tour進行を純粋関数化 | 必要最小限のAPI、UMD/CommonJS互換の要否 |
| `index.html` | 変更 | ARIAと必要script読込 | `role="log"` の置換、既存hookの互換性 |
| `styles.css` | 変更 | title、progress、tour controls、animation | 既存世界観、44px、モバイルoverflow |
| `tests/chatbot-search.test.cjs` | 変更 | versionとデータ整合ゲート更新 | 既存検索回帰を残す |
| `tests/chatbot-tour.test.cjs` | 新規候補 | tour境界と状態遷移の単体テスト | DOMなしで検証可能にする |
| `README.md` | 変更 | 1.1仕様・編集方法・テスト方法 | 1.1候補表記を完成仕様へ更新 |

削除候補はない。Fuse.jsとライセンスは変更しない。

## 6. リスクと未確定事項

### 確定済み

- 標準ツアーは1本、7ステップ。
- V文化論は既存intentを使い、初期テーマとツアー双方へ置く。
- ワンダーメイクフェス5動画は既存URLを使い、本人性を直接説明しない。
- 状態はページ内だけ保持し、reload後は初期化。
- 既存検索と自由入力は残す。

### Terraで詳細化する事項

- `tour-core.js` を新設するか、十分に小さければ `chatbot.js` 内の純粋関数に留めるか。
- ボタン群の具体的DOM順、class名、状態名、関数名。
- ツアー固有補足をstepに持たせるか、既存回答だけで成立させるか。
- aria-liveの具体的マークアップと、カード差替え時の読み上げ重複回避。

### 要確認

- YouTube公開ページは今回のオンライン取得ではスロットリングされ、HTTP表示を直接再確認できなかった。動画ID、タイトル、時刻はリポジトリ内の証拠カードと現行 `knowledge.json` で一致している。Lunaの公開前確認でブラウザから再生可能性を1回確認すること。
- V文化論のリンクは現在noteトップ。1.1で特定記事へ深くリンクする場合は、公開中の代表記事URLを別途一次確認すること。確認できなければトップリンクを維持する。

## 7. 受け入れ条件

- [ ] 右下からナビを開閉でき、既存サイト閲覧を妨げない。
- [ ] 初期カードの最優先位置からおすすめツアーを開始できる。
- [ ] 7ステップを順に最後まで進められる。
- [ ] 2ステップ目以降で前へ戻れ、先頭・末尾で範囲外へ進まない。
- [ ] 任意ステップでツアーを終了し、別テーマまたは最初からへ移れる。
- [ ] ツアー中の進捗が視覚・読み上げの両方で分かる。
- [ ] 質問・回答ログを長く積まず、主回答カードは常時1枚で切り替わる。
- [ ] 自由入力と既存15カテゴリの検索が回帰していない。
- [ ] 「V文化論ってなに？」に分かりやすく回答し、うのっちの活動との関係が伝わる。
- [ ] ワンダーメイクフェス5動画へ安全な別タブリンクで移動できる。
- [ ] 動画紹介文に「実写のうのっち」「中の人」「顔出し」等の直接表現がない。
- [ ] 内部リンクは実在アンカーへ移動し、外部リンクは `noopener noreferrer` を持つ。
- [ ] 閉じて同一ページ内で再度開くと直前カード・tour位置が保たれる。
- [ ] reload後は初期カードから始まり、永続トラッキングを行わない。
- [ ] 390×844および狭幅で横overflowがなく、主要操作のタッチ領域が44px以上。
- [ ] キーボード、Escape、フォーカス復帰、reduced-motionが機能する。
- [ ] データ読込失敗時に理解可能なエラーを表示する。
- [ ] Nodeテストがすべて通り、サイト主要セクションが壊れていない。

## 8. Terraへの引き継ぎ判断

Sol工程は完了。製品上の大きな判断（ツアー数、順序、範囲、状態保持、非対象、動画表現、技術制約）は確定している。Terraは仕様を再決定せず、コンポーネント、データスキーマ、状態遷移、操作、ARIA、ファイル別実装単位を具体化し、Luna向け実装プロンプトへ落とし込める。

## 9. Terra向け完成プロンプト

---

あなたはTerraです。静的GitHub Pages `sanpeita.github.io` に実装済みの「うのっちナビ」を、1.0から1.1へ引き上げるための詳細設計を担当してください。コード実装はせず、Lunaが追加判断なしで実装できる設計と実装プロンプトを作成してください。

### プロジェクトの目的

ポートフォリオ右下の案内役として、初見閲覧者が質問を考えなくても、うのっちの人物像、VTuber活動、制作、教育、デジファブ、V文化論、実績のつながりを理解できる「おすすめツアー」を追加します。一般的なAIチャットや業務チャットボットではなく、既存ポートフォリオの世界観に溶け込む回答カード体験にしてください。

### Solが確認した現状

- 静的HTML/CSS/JavaScript。フレームワーク、package管理、ビルド、Lintなし。
- GitHub Pagesの公開ブランチは `master`。
- `index.html`: 右下widget DOM。
- `styles.css`: 既存デザインとレスポンシブ。mobileは左右12px、panel最大78dvh、close 44px。
- `assets/chatbot/knowledge.json`: version 1.0.0、15 intents、facts/answers/links/suggestions。
- `assets/chatbot/chatbot.js`: 開閉、append型カード描画、入力、外部/内部リンク。
- `assets/chatbot/search-core.js`: Fuse + keywordによる日本語検索、回答選択。
- `tests/chatbot-search.test.cjs`: 現在7系統の受け入れテスト。
- 外部AI APIや質問送信はなく、ブラウザ内だけで動作。
- `v-culture` intentと `events-awards` intentはすでに存在。
- ワンダーメイクフェス5動画は `https://www.youtube.com/watch?v=ZgVxVa9wKUo&t=101s`。紹介はイベント、toio、子どもとの活動、教育・制作の文脈に限定し、本人の実写、顔出し、中の人等を言わない。

### 確定仕様

1. 初期画面の主操作は「おすすめツアーをはじめる」。
2. 標準ツアーは1本、次の7step。
   1. `profile-overview` — まず、うのっちについて
   2. `vtuber-history` — VTuberを表現の器に
   3. `representative-works` — 遊びと技術を作品に
   4. `digital-education` — 難しそうを、学べそうに
   5. `digital-fabrication` — 画面の中から、手で触れるものへ
   6. `v-culture` — 活動をつなぐV文化論
   7. `events-awards` — 教育・toio・実績が交わる場所
3. 操作は「次を見る」「ひとつ前に戻る」「ツアーを終了」。完了後は「最初から見る」「別のテーマを見る」。
4. ツアー中の自由入力は、確認なしでツアーを終了して個別回答へ移る。
5. チャットログを積まず、welcome、個別回答、tour step、tour complete、errorのいずれも主カード1枚を差し替える。
6. カードはeyebrow/進捗、title、body、links、actionsを持つ。
7. 同一ページで閉じて再度開くと直前カード・tour位置を保持する。reload後は初期化。localStorage、sessionStorage、URL stateは使わない。
8. 自由入力と既存15 intentは維持する。
9. 外部リンクは別タブ + `noopener noreferrer`。内部アンカーは実在するものだけを使う。
10. 外部AI API、生成回答、追跡、動画埋め込み、複数分岐ツアー、サイト本文の新規セクションは対象外。

### コンテンツ制約

- 初期文: 「こんにちは。うのっちの活動を、作品・教育・V文化論のつながりからご案内します。初めての方は、おすすめツアーからどうぞ。」
- 初期テーマ: `profile-overview`、`representative-works`、`v-culture`、`digital-education`。
- step 3でUnity、Blender、Roblox、Minecraft、toioの広がりを一文で示す。
- step 6でTED世界共創計画を関連テーマとして短く触れる。独立intentは作らない。
- step 7で公式ブースレポート動画を自然に案内する。
- 本文は160〜240字程度を上限目安、link最大3、関連テーマ最大3。
- toneは文化系・教育系・フレンドリー。過剰なAI感、サポート窓口感を避ける。

### 技術方針

- 既存構造を維持し、依存追加・フレームワーク導入をしない。
- `knowledge.json` を `1.1.0` にし、`welcome` と `tours` を追加する。
- welcome操作とtour stepは質問文検索ではなくintent IDを直接参照する。
- tour stepは既存intent本文を再利用し、本文重複を避ける。
- `chatbot.js` はstate → 単一view renderへ整理する。
- `search-core.js` は原則変更しない。
- 推奨として小さな `assets/chatbot/tour-core.js` を検討し、tour境界とstep取得をDOMから分離する。不要なら `chatbot.js` 内の純粋関数でもよい。判断理由を書くこと。
- 主要概念状態: panel、view (`loading/welcome/answer/tour/tour-complete/error`)、currentIntentId、tourId、tourIndex、previousAnswerIds。

### UI・アクセシビリティ制約

- 既存の色、フォント、アバター、角丸、影、右下配置を維持。
- mobile左右12px、safe-area、close 44pxを壊さない。全画面常時表示にしない。
- 主操作は44px以上。進捗を視覚と読み上げの双方で示す。
- append型の `role="log"` は単一カードに適切なaria-live構成へ変更する。
- launcher `aria-expanded/controls`、Escape、閉じた後launcherへfocusを戻す挙動を維持。
- card切替は150〜220ms程度の軽いopacity + 数px移動。reduced-motionでは無効化。
- カード切替で入力フォーカスを勝手に奪わない。表示領域は先頭へ戻す。

### エラー要件

- JSON取得失敗やスキーマ不正は、理解可能なエラーカードを表示。
- tourの参照intent欠落はテストで公開前に失敗させる。
- 空・不正URLは描画せず、テストで検出。

### 変更対象

- 変更: `assets/chatbot/knowledge.json`, `assets/chatbot/chatbot.js`, `index.html`, `styles.css`, `tests/chatbot-search.test.cjs`, `README.md`
- 新規候補: `assets/chatbot/tour-core.js`, `tests/chatbot-tour.test.cjs`
- 削除なし。Fuse関連ファイルは変更しない。

### テスト要件

- version 1.1.0、既存15 intent維持、welcome参照、7stepと参照整合、tour境界。
- V文化論とワンダーメイクフェス5検索の回帰。
- 動画ID `ZgVxVa9wKUo` と101秒指定。
- 禁止表現、内部anchor、外部URL、suggestion解決の既存ゲート維持。
- 手動でdesktop、390×844、狭幅、keyboard、Escape、focus、reduced-motion、tour完走・戻る・終了・閉じて再開を確認。
- 公開前に動画リンクをブラウザで1回再生確認する（今回のSolオンライン取得はYouTube側スロットリングで直接確認できず、リポジトリ証拠と現行データの一致までは確認済み）。

### Terraが作る成果物

次の順で、具体的な詳細設計を作成してください。

1. コンポーネント/描画関数構成図
2. `knowledge.json` の完全な1.1スキーマ例
3. 状態一覧と状態遷移表
4. 7stepすべての表示要素（見出し、本文の採用方針、補足、links、actions）
5. welcome、個別回答、fallback、tour complete、error各カード仕様
6. 全ボタン・リンク・keyboard操作一覧
7. mobile/desktop寸法・配置仕様
8. animationとreduced-motion仕様
9. ARIA、focus、読み上げ仕様
10. 読込・データ不正・リンク不正の挙動
11. ファイル別変更計画（変更する関数・データキーまで）
12. Lunaが安全に順番実装できるタスク分割
13. Luna向け完成実装プロンプト
14. 自動・手動の完了判定チェックリスト

コンポーネント名、状態名、props相当、イベント名、CSS class、関数責務はTerraが決定してください。仕様上の大きな判断は上記で確定済みです。要確認事項は推測で埋めず、明示してください。

---
